# =====================================================================
# Nirmana-Shell - Transpiled Theme: catppuccin_macchiato
# Ported from Oh My Posh JSON schema via Nirmana OMP Transpiler
# =====================================================================

scan_timeout = 30
command_timeout = 1000
add_newline = true

format = """
$username\
$directory\
$git_branch\
$git_status\
$line_break\
$character\
"""

[username]
show_always = true
format = "[ ](#ff70a6)[$user](p:blue) "

[directory]
format = "[ $path](p:pink) "
truncation_length = 3
truncation_symbol = "…/"

[git_branch]
format = "[ $branch](p:lavender)"

[git_status]
format = "([$all_status$ahead_behind](p:lavender) )"
modified = " "
staged = " "
stashed = " "
ahead = " ⇡${count}"
behind = " ⇣${count}"
diverged = " ⇕⇡${ahead_count}⇣${behind_count}"

[character]
success_symbol = "[❯](bold green) "
error_symbol = "[❯](bold red) "
