# =====================================================================
# Nirmana-Shell - Transpiled Theme: catppuccin_macchiato
# Ported from Oh My Posh JSON schema via Nirmana OMP Transpiler
# =====================================================================

scan_timeout = 30
command_timeout = 1000
add_newline = true

format = """
$username\
$directory\
$git_branch\
$git_status\
$line_break\
$character\
"""

[username]
show_always = true
format = "[ ](#ff70a6)[$user](#8AADF4) "

[directory]
format = "[ $path](#F5BDE6) "
truncation_length = 3
truncation_symbol = "…/"

[git_branch]
format = "[ $branch](#B7BDF8)"

[git_status]
format = "([$all_status$ahead_behind](#B7BDF8) )"
modified = " "
staged = " "
stashed = " "
ahead = " ⇡${count}"
behind = " ⇣${count}"
diverged = " ⇕⇡${ahead_count}⇣${behind_count}"

[character]
success_symbol = "[❯](bold green) "
error_symbol = "[❯](bold red) "
